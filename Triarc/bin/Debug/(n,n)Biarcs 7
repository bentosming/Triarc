(2,2) with facesSizes 7 doesn't exists
(3,3) with facesSizes 7 doesn't exists
(4,4) with facesSizes 7 doesn't exists
(5,5) with facesSizes 7 doesn't exists
(6,6) with facesSizes 7 doesn't exists
(7,7) with facesSizes 7 doesn't exists
(8,8) with facesSizes 7 doesn't exists
(9,9) with facesSizes 7 doesn't exists
(10,10) with facesSizes 7 doesn't exists
